#Lists is a collection of elements, that can be of different types.
#vectors can be elements of lists
#we are creating it using the command list()
x=c(3,5,2,7,9,4)
xList=list(3, x, c(TRUE,FALSE,FALSE), "Marko", seq(1, by=0.5, length.out=10))
xList
xList[3]